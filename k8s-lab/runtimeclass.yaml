apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: runsc-warp
# Matches the containerd runtime handler named "runsc" that
# inject-warp-runtime.sh adds to each kind node's config.toml.
handler: runsc
